def binary_search(sorted_list, item):
    if isinstance(sorted_list, list):
        start = 0
        end = len(sorted_list)
        index = (start + end) / 2
        while start <= end:
            pass  # TODO
